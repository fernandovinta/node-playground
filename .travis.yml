language: node_js  
node_js: "node"  
services: mongodb  
cache:  
  directories:
    - "node_modules"
  yarn: true
install:
    - npm install -g nyc
    - npm install -g codecov
script:
    - nyc ./node_modules/mocha/bin/_mocha --reporter lcovonly -- -R spec
    - codecov